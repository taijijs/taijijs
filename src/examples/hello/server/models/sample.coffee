Model = require('taiji').Model

ObjModel = Model({})

obj = new ObjeModel()

User  = Model name: ''

user = new User

Users = Model([User])

users = new Users([user])

Number = Model(0)

one = Number(1)

